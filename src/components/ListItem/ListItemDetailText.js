import { Text as RNText } from 'react-native';
import withStyles from '@blankapp/ui/src/withStyles';

const ListItemDetailText = withStyles('ListItemDetailText')(RNText);

export default ListItemDetailText;
