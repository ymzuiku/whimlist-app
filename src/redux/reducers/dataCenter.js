// import * as types from '../types';

const initialState = {
};

const dataCenter = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default dataCenter;
